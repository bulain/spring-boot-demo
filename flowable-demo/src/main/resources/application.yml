server:
  port: 8081
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mybatis_dev?useSSL=false&serverTimezone=PRC&characterEncoding=utf-8
    username: mybatis
    password: dev
flowable:
  database-schema-update: true
  idm:
    enabled: true

logging:
  level:
    root: WARN
    org.springframework: INFO
    com.bulain: DEBUG

